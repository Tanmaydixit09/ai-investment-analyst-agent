// Placeholder shell. MainLayout + DashboardPage are wired in once the backend
// (steps 2-9 of the build plan) is in place and there's a real API to call.
export default function App() {
  return (
    <div className="min-h-screen bg-slate-950 text-slate-100 flex items-center justify-center">
      <p className="text-lg">Investment Research Agent — scaffold ready.</p>
    </div>
  );
}
